/**
 * test/run.ts — entry point for `npm test`. Imports every suite so Node's
 * built-in test runner (node:test) collects and runs them in one process.
 */
import "./brief.test";
import "./references.test";
import "./site-dna.test";
