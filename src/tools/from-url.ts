/**
 * Tool: generate_from_url
 *
 * Placeholder. Real implementation arrives in AC-9.
 */
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerFromUrl(_server: McpServer): void {
  // Intentionally empty — implemented in AC-9.
}
