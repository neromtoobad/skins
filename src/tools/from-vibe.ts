/**
 * Tool: generate_from_vibe
 *
 * Placeholder. Real implementation arrives in AC-8. This stub returns a
 * not-implemented error so the build is consistent and the type surface
 * is fixed.
 */
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerFromVibe(_server: McpServer): void {
  // Intentionally empty — implemented in AC-8.
}
