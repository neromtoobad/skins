/**
 * Tool: generate_from_image
 *
 * Placeholder. Real implementation arrives in AC-10.
 */
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerFromImage(_server: McpServer): void {
  // Intentionally empty — implemented in AC-10.
}
